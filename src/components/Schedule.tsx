import React from 'react';

const Schedule = () => {
  return (
    <section id="schedule" className="py-20 text-center">
      <h2 className="text-3xl font-semibold">Schedule Section</h2>
      <p>Event timeline and sessions.</p>
    </section>
  );
};

export default Schedule;
