import React from 'react';

const GifWall = () => {
  return (
    <section id="gif-wall" className="py-20 text-center">
      <h2 className="text-3xl font-semibold">Gif Wall Section</h2>
      <p>A wall of GIFs instead of a standard carousel.</p>
    </section>
  );
};

export default GifWall;
